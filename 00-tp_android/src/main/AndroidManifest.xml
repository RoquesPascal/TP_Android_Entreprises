<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a00_tp_android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".HistoriqueDetailsActivity" android:exported="true" android:label="@string/LabelMenuHistorique"/>
        <activity android:name=".HistoriqueActivity" android:exported="true" android:label="@string/LabelMenuHistorique"/>
        <activity android:name=".FormulaireEntrepriseActivity" android:exported="true" android:label="@string/LabelMenuEntrepriseRecherche"/>
        <activity android:name=".EntrepriseActivity" android:exported="true" android:label="@string/LabelMenuEntrepriseAffichage"/>
        <activity android:name=".MainActivity" android:exported="true" android:label="@string/LabelMenuPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>